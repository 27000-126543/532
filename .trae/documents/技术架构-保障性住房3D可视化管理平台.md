## 1. 架构设计

```mermaid
flowchart TB
    subgraph "前端展示层"
        A["React 18 SPA"]
        A1["3D可视化模块 (React Three Fiber)"]
        A2["UI面板组件 (TailwindCSS)"]
        A3["状态管理 (Zustand)"]
        A4["动画与特效 (Framer Motion / Three.js)"]
    end
    
    subgraph "服务层 (Mock)"
        B["Mock API Service"]
        B1["房屋住户数据"]
        B2["门禁人脸数据"]
        B3["审批工作流"]
        B4["报修工单数据"]
        B5["能耗统计数据"]
        B6["配租摇号数据"]
    end
    
    subgraph "数据层"
        C["本地Mock数据 (JSON)"]
        C1["房屋/住户/租约"]
        C2["门禁/预警记录"]
        C3["审批/工单记录"]
        C4["能耗/报表数据"]
    end
    
    subgraph "外部依赖"
        D["Three.js + React Three Fiber"]
        D1["xlsx (Excel导出)"]
        D2["lucide-react (图标)"]
    end
    
    A --> A1
    A --> A2
    A --> A3
    A --> A4
    A1 --> D
    A2 --> D2
    A --> B
    B --> B1
    B --> B2
    B --> B3
    B --> B4
    B --> B5
    B --> B6
    B --> C
    A4 --> D
```

## 2. 技术描述

- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite 5
- **样式方案**：TailwindCSS 3 + PostCSS
- **3D渲染**：Three.js + @react-three/fiber + @react-three/drei + @react-three/postprocessing
- **状态管理**：Zustand（轻量状态管理）
- **动画**：Framer Motion（UI动画）+ Three.js Animation（3D动画）
- **图表**：自定义SVG图表（能耗统计、审批流程）
- **图标**：lucide-react
- **Excel导出**：xlsx (SheetJS)
- **后端**：无后端，使用本地Mock数据模拟

## 3. 路由定义

| 路由 | 用途 |
|------|------|
| / | 3D可视化大屏主入口（社区全景视图） |
| /building/:id | 楼栋透视视图 |
| /house/:id | 房屋详情与户型视图 |
| /approval | 租金补贴三级审批中心 |
| /allocation | 配租管理（摇号+选房） |
| /repair | 报修工单调度中心 |
| /energy | 能耗统计管理 |
| /warnings | 预警中心（空置/转租） |
| /reports | 运营报表导出 |
| /logs | 操作日志与登录审计 |

## 4. 数据模型

### 4.1 数据模型定义（ER图）

```mermaid
erDiagram
    BUILDING ||--o{ HOUSE : "包含"
    HOUSE ||--o{ TENANT : "租住"
    HOUSE ||--o{ RENT_RECORD : "产生"
    HOUSE ||--o{ ACCESS_RECORD : "产生"
    HOUSE ||--o{ REPAIR_ORDER : "产生"
    HOUSE ||--o{ WARNING : "触发"
    BUILDING ||--o{ ENERGY_STAT : "统计"
    TENANT ||--o{ SUBSIDY_APPROVAL : "申请"
    SUBSIDY_APPROVAL ||--|| APPROVAL_NODE : "流转"
    ALLOCATION_APPLICANT ||--o| LOTTERY_RESULT : "中签"
    USER ||--o{ OPERATION_LOG : "操作"

    BUILDING {
        string id PK
        string name
        int floors
        int totalHouses
        float energyBudget
        string district
        string street
    }

    HOUSE {
        string id PK
        string buildingId FK
        string unit
        string floor
        string roomNo
        float area
        float monthlyRent
        string status
        string layoutType
    }

    TENANT {
        string id PK
        string houseId FK
        string name
        string idCard
        int familySize
        float monthlyIncome
        date leaseStart
        date leaseEnd
        float subsidyRatio
    }

    RENT_RECORD {
        string id PK
        string houseId FK
        string month
        float amount
        date payDate
        string status
    }

    ACCESS_RECORD {
        string id PK
        string houseId FK
        datetime timestamp
        string personName
        string faceMatchResult
        string direction
    }

    REPAIR_ORDER {
        string id PK
        string houseId FK
        string faultType
        string description
        string status
        datetime createTime
        string workerId
        datetime acceptTime
        datetime finishTime
    }

    WARNING {
        string id PK
        string houseId FK
        string type
        string level
        string status
        datetime createTime
        string description
    }

    ENERGY_STAT {
        string id PK
        string buildingId FK
        string month
        float consumption
        float budget
    }

    SUBSIDY_APPROVAL {
        string id PK
        string tenantId FK
        float newSubsidyRatio
        float oldSubsidyRatio
        string status
        string currentStage
    }

    APPROVAL_NODE {
        string id PK
        string approvalId FK
        string stage
        string approver
        string result
        datetime time
        string comment
    }

    ALLOCATION_APPLICANT {
        string id PK
        string name
        float score
        int rank
    }

    LOTTERY_RESULT {
        string id PK
        string applicantId FK
        string houseId FK
        datetime lotteryTime
    }

    USER {
        string id PK
        string name
        string role
        string department
    }

    OPERATION_LOG {
        string id PK
        string userId FK
        string action
        datetime time
        string ip
    }
```

### 4.2 目录结构设计

```
src/
├── components/
│   ├── ui/                    # 通用UI组件（按钮、卡片、抽屉等）
│   ├── three3d/              # 3D场景组件
│   │   ├── CommunityScene.tsx   # 社区全景
│   │   ├── BuildingScene.tsx    # 楼栋透视
│   │   ├── HouseScene.tsx       # 户型详情
│   │   ├── Building.tsx         # 单楼栋模型
│   │   ├── HouseUnit.tsx        # 单房屋模型
│   │   └── Effects.tsx          # 后处理特效
│   ├── panels/               # 信息面板组件
│   │   ├── HouseDetailPanel.tsx
│   │   ├── StatsDashboard.tsx
│   │   ├── ApprovalFlowPanel.tsx
│   │   └── WarningListPanel.tsx
│   ├── modules/              # 功能模块组件
│   │   ├── LotteryScreen.tsx
│   │   ├── RepairDispatch.tsx
│   │   ├── EnergyChart.tsx
│   │   └── ReportExport.tsx
│   └── layout/               # 布局组件
│       ├── TopNav.tsx
│       ├── SidePanel.tsx
│       └── StatusBar.tsx
├── store/                    # Zustand状态管理
│   ├── useHouseStore.ts
│   ├── useApprovalStore.ts
│   ├── useWarningStore.ts
│   └── useUserStore.ts
├── data/                     # Mock数据
│   ├── buildings.ts
│   ├── houses.ts
│   ├── tenants.ts
│   ├── accessRecords.ts
│   ├── repairOrders.ts
│   ├── approvals.ts
│   └── energyStats.ts
├── services/                 # 业务服务层
│   ├── api.ts
│   ├── approvalService.ts
│   ├── warningService.ts
│   └── exportService.ts
├── types/                    # TypeScript类型定义
│   └── index.ts
├── utils/                    # 工具函数
│   ├── helpers.ts
│   └── constants.ts
├── App.tsx
├── main.tsx
└── index.css
```

## 5. 核心技术决策说明

1. **React Three Fiber vs 原生Three.js**：采用R3F将Three.js能力以声明式React组件方式暴露，便于状态驱动3D场景，与UI组件共享状态。
2. **Zustand作为状态管理**：相比Redux更轻量，避免样板代码，支持跨组件共享3D场景与业务面板状态。
3. **玻璃拟态UI设计**：TailwindCSS + backdrop-blur + 半透明渐变实现科技感面板，与深色3D场景形成层次。
4. **后处理栈**：Bloom泛光突出楼栋状态色边框，SSAO增强空间感，Vignette聚焦视口中心。
5. **InstancedMesh优化**：同户型大量重复房屋使用实例化网格，降低Draw Call，保证5万面级别场景60fps。
6. **本地Mock优先**：所有后端接口以Mock数据形式实现，确保前端独立可运行，后续可无缝替换为真实API。
